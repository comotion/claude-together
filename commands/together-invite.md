---
description: Open a pairing rendezvous to share with a friend (Claude Together)
argument-hint: <room-name>
---

Use the claude-together MCP server's create_invite tool with room_name "$ARGUMENTS" (if no name was given, use a short sensible name based on what we're working on). Show me the rendezvous id prominently. Tell me it is NOT a secret and does not expire, so I can paste it anywhere convenient. Remind me that when my friend answers, we will each see a six-digit number that we must compare out of band (a call, not the channel I sent the id in), and that the pairing only completes when we both confirm the same number. I must keep this session open until then.
